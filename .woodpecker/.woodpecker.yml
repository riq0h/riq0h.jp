clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      recursive: true
      partial: false
      depth: 1

steps:
  build:
    image: hugomods/hugo:base
    commands:
      - hugo --noBuildLock

  deploy:
    image: drillster/drone-rsync
    settings:
      hosts:
        from_secret: HOST
      user:
        from_secret: USER
      key:
        from_secret: SSH
      port:
        from_secret: PORT
      source: public
      target: /var/www/html
