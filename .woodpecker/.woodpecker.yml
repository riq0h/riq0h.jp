clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      recursive: true

steps:
  build:
    image: klakegg/hugo:ext-alpine
    commands:
      - hugo -D
  deploy:
    image: appleboy/drone-scp
    secrets: [SSH]
    settings:
      host:
       - mystech.ink
      username: riq0h
      password:
        from_secret: SSH
      port: 4545
      target: /home/riq0h/
      source:
        - public
